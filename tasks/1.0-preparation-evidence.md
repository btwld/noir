# 1.0 private-preparation evidence

Captured: 2026-08-11T20:10:16Z.

This record closes the evidence work in Phase 1 of
[`1.0-release-plan.md`](1.0-release-plan.md). It preserves the reviewed
`1.0.0-alpha.1` candidate without authorizing a history replacement, native
artifact change, visibility change, workflow run, tag, release, or
publication.

## Candidate identity and repository state

- Preparation commit:
  `c5794b6f104a201a7e9dce9a0955417e7f1ea602`.
- Preparation tree: `77ebb4e571494d9ae9ed9edd8beab28dc4a09a63`.
- Commit timestamp: `2026-08-11T16:00:09-04:00`.
- Workspace branch at capture: `leoafarias/commit-and-push`, tracking
  `origin/leoafarias/havana-v1` with zero divergence. The workspace branch was
  not renamed and the preparation branch was not checked out in another
  worktree.
- `origin`: `git@github.com:leoafarias/noir.git`.
- GitHub state: private repository; default branch `master`; Actions disabled;
  no pull requests, tags, or releases.
- `origin/HEAD -> origin/master`; `origin/main` did not exist.
- The working tree was clean after
  `git submodule update --init --recursive external/opentui` and
  `dart pub get`.
- `pubspec.lock` was ignored and untracked. `.conductor/settings.toml` was
  tracked and its noninteractive setup command completed successfully.

## Offline recovery and source archive

The recovery artifacts are intentionally gitignored under
`.context/recovery/` in the preparation workspace.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `noir-prep-c5794b6.bundle` | 3,710,345 | `5d2c4ecf754c954432e0895d7315fa7e9c36002671ef2660f7190bf30a31755b` |
| `noir-prep-c5794b6.tar` | 12,165,120 | `8444dd9e739977c8fb1ebd084b80a89fb7ee9d9cfa26b765e44424171c1d208c` |
| `noir-prep-c5794b6.archive-inventory.txt` | 23,457 | `d9ce36b43e3b450c8b89fe0d441a34101f09510a9d420f385dc8a868038a813e` |

`git bundle verify` reported a complete SHA-1 history containing exactly these
refs:

```text
c5794b6f104a201a7e9dce9a0955417e7f1ea602 HEAD
dbf4d30ebfd9a119f1b0306a30d9900561e8de2e refs/heads/master
```

The source archive was produced with:

```sh
git archive --format=tar \
  --output=.context/recovery/noir-prep-c5794b6.tar \
  c5794b6f104a201a7e9dce9a0955417e7f1ea602
```

Its sorted inventory has 617 members: 70 directory entries and 547
non-directory entries. It contains neither `.git`, `.context`, nor
`pubspec.lock`. The candidate tree has 548 tracked entries; the one deliberate
difference is the `external/opentui` gitlink, which `git archive` does not
embed. Recovery must initialize that submodule at the separately verified pin
below.

## Pinned native inputs

`external/opentui` was clean at
`ddbc9edf81a1fa89961135ab0481df15054ed4b0`. The six native files matched
`native_manifest.json` without modification:

| Target | SHA-256 |
| --- | --- |
| macOS x64 | `d80970770863f647cca83dbe1ab983039660a3593da01552620f4f0510bd952a` |
| macOS arm64 | `0648d6243c43f8780f2e41b66052478a9821a0986385b961f11a0536059053bf` |
| Linux x64 | `63535b6958c9fad3f59b37b4dcabb231009a83488ff578059cb8e46f31d05e04` |
| Linux arm64 | `7c13f55f2b30d7da9753b33fd957ac3afbcf9083128707a8bb9d809ebf402bf1` |
| Windows x64 | `221c8acb12da85f279621e13d8d0e45e4718a00bb13d5fcc940f1cb686a715fa` |
| Windows arm64 | `9593cdeab209116de3ef8b58eefb3942bcb133d0a60fd844fdaf0e8c1cb2238e` |

## History and stale-reference scan

The retained history is the two commits reachable from the preparation
commit. Gitleaks 8.30.1 scanned both commits, approximately 3.77 MB, with full
redaction enabled and reported no leaks.

Repository, branch, pull-request, tag, and release references were inventoried
in both retained trees and compared with the live private repository:

- The preparation tree's repository URLs resolve only to the intended Noir
  repository and the pinned `leoafarias/opentui` fork. The `sst/opentui` Go
  module path is part of the pinned parity helper and is not package metadata.
- The initial private commit contains generic OpenTUI skill links and an old
  session record. Both were removed by the preparation commit and are retained
  only as private recovery history.
- The preparation tree has 17 README `/blob/master/` links and two CI
  `branches: [master]` filters. They match the live default branch and are
  explicitly scheduled for replacement immediately before Phase 3 creates
  `main`; changing them during Phase 1 would make the private repository links
  stale now.
- `main`, version tags, and GitHub releases appear only as future operations or
  safeguards in the authoritative release plan and workflow templates. There
  were no numbered pull-request, issue, or release-tag links in the candidate,
  and the live repository had no pull requests, tags, or releases.

No retained-history finding requires a Phase 1 source change or history
rewrite.

## Tool versions and dated checks

The preservation audit ran locally on macOS 26.5.2 (Darwin 25.5.0, arm64)
with:

- Git 2.55.0;
- Dart SDK 3.10.7 stable on macOS arm64;
- GitHub CLI 2.97.0;
- Gitleaks 8.30.1;
- ShellCheck 0.11.0;
- Pana 0.23.12;
- Go 1.26.5;
- bsdtar 3.5.3 / libarchive 3.7.4;
- `shasum` 6.02.

Actionlint was not installed in the current shell, so it was not rerun for
this documentation-only preservation record. The reviewed candidate result in
[`release-readiness.md`](release-readiness.md) records Actionlint 1.7.12 as
passing; no workflow file changed in this slice.

On 2026-08-11 UTC, the following preservation checks passed:

- `git fetch origin --prune` and zero-divergence comparison with
  `origin/leoafarias/havana-v1`;
- `git bundle verify .context/recovery/noir-prep-c5794b6.bundle`;
- source-archive SHA-256 and complete sorted-inventory checks;
- manifest-to-file SHA-256 comparison for all six native artifacts;
- `dart run scripts/fetch_opentui_binaries.dart --verify-only`, verifying the
  manifest and all six binaries;
- `dart test test/architecture/ --concurrency=1`, with all 148 tests passing;
- local Markdown-target checks, a focused Gitleaks scan of `tasks/`, and
  `git diff --check` for this documentation-only evidence slice;
- clean submodule-pin and working-tree checks;
- live GitHub privacy, default-branch, Actions, branch, pull-request, tag, and
  release inspection;
- full retained-history Gitleaks and stale-reference scans.

The broader candidate verification remains recorded in
[`release-readiness.md`](release-readiness.md). Restricted terminal, PTY,
signal, native-build, workflow, tag, release, visibility, and publication
operations were not run.

## Phase 1 disposition

All six Phase 1 tasks have repository or recovery-artifact evidence:

1. the reviewed candidate commit is present and pushed on the private
   preparation branch;
2. the complete offline bundle is verified;
3. candidate identity, archive inventory and hash, native hashes, tools, and
   dated checks are recorded here;
4. `pubspec.lock` remains ignored and untracked;
5. `.conductor/settings.toml` remains tracked and its setup succeeds;
6. the entire retained history has secret and stale-reference evidence.

At capture time, Phase 2 was blocked pending exact native-build authorization.
The later authorization and approved method are recorded in
[`2.0-native-build-contract.md`](2.0-native-build-contract.md).
