* text=auto eol=lf

*.dylib binary
*.dll binary
*.so binary

# Golden snapshots are compared with LF normalization.
test/goldens/** text eol=lf
# Buffer snapshots intentionally preserve fixed-width terminal padding.
test/goldens/*.buffer.txt whitespace=-trailing-space
