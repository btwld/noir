* text=auto eol=lf

*.dylib binary
*.dll binary
*.so binary

# Preserve exact license bytes extracted from the verified official archives.
packages/noir/third_party/opentui-v0.5.1/** -whitespace

# Golden snapshots are compared with LF normalization.
packages/noir/test/goldens/** text eol=lf
# Buffer snapshots intentionally preserve fixed-width terminal padding.
packages/noir/test/goldens/*.buffer.txt whitespace=-trailing-space
